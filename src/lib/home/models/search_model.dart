class SearchModel {
  final String projectName, status, statusColor, typeProject;

  SearchModel(
      {required this.projectName,
      required this.status,
      required this.statusColor,
      required this.typeProject});
}
